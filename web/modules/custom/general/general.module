<?php
use Drupal\Core\Form\FormStateInterface;
function hook_form_alter(&$form, FormStateInterface $form_state, $form_id) {
    var_dump($form_id);
    if ($form_id == 'node_events_form') {
        $form['field_price']['status'] = [
            'invisible' => [
                ':input[name="field_cost"]'=> ['!value' => 1]
            ]
            ];
            }
        }